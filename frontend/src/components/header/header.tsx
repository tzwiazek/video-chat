import React from 'react';
import { HeaderContainer, VideoChatTitle } from './header.styles';

const Header = () => {
  return (
    <HeaderContainer>
      <VideoChatTitle>Video Chat Meeting</VideoChatTitle>
    </HeaderContainer>
  );
};

export default Header;
